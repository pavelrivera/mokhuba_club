// Vanilla JS module generated from index.vue
(function(){
  class State {
    constructor(){
    }
  }
  const state = new State();
  function wireEvents(root){
  }
  document.addEventListener('DOMContentLoaded',()=>{
    const root = document;
    wireEvents(root);
  });
})();